server:
  port: 8082
 
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/amazon_microservices
    username: root
    password: "Bidyut@07?"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://eureka-discovery-server:8764/eureka
    register-with-eureka: true
    fetch-registry: true
